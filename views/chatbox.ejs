<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>REST Chat App-MongoDB</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <div class="chat-container">
        <div class="chat-header">Chatbox</div>
        <div class="chat-box">
            <% for (chat of chatData) { %>
                <div class="message data">
                    <div class="ctrl-btn">
                        <a href="http://localhost:8080/<%= chat._id %>/edit">Edit &#128393;</a> 
                        <a href="http://localhost:8080/<%= chat._id %>/delete">Delete &#128473;</a>
                    </div>
                    <p><strong>From:</strong> <%= chat.from %> </p>
                    <p> <%= chat.msg %> </p>
                    <p class="message-info"><strong>To:</strong> <b><%= chat.to %></b> | <%= chat.created_at.toString().split(" ")[4] %> | <%= chat.created_at.toString().split(" ").slice(0,4).join(" ") %></p>
                </div>
            <% } %>
        </div>
        <div class="chat-input">
            <a href="http://localhost:8080/new"><button>Type new message</button></a>
        </div>
    </div>
</body>
</html>
